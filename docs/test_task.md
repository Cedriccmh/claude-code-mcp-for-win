# Multi-Step Test Task

Please complete the following multi-step task:

1. Create a directory called `claude-cli-test` in the current workspace
2. Inside that directory, create a file called `step1.txt` with the content "Step 1 completed"
3. Create a file called `step2.json` with a JSON object containing:
   - `task`: "multi-step test"
   - `status`: "in_progress"
   - `steps_completed`: 2
4. Create a simple Node.js script called `validator.js` that reads step2.json and logs whether the status is "in_progress"
5. Create a README.md file documenting all the files created
6. List all files in the claude-cli-test directory to verify everything was created

After completing all steps, provide a summary of what was created.
